# 预设管理器路线图

本文档是当前唯一的功能路线和 UI 改造任务账本。旧方案文档只作为历史参考，归档在 `docs/archive/`。

## 工作规则

- 每次开始任务前先检查本文档。
- 涉及酒馆助手开发时，Codex 必须手动读取 `AGENTS.md` 引用的 `.cursor/rules/*.mdc` 规则文件。
- 新需求先归类为“当前节点 / 下个节点 / 暂缓”，再改代码。
- 每完成一个稳定节点，更新本文档、构建验证、提交 git。
- 正式发布前版本号统一使用 `v0.x.x`；功能和 UI 稳定后再从 `v1.0.0` 开始。
- 新增项目 Markdown 默认使用中文。

## 当前设计基准

- 小窗默认尺寸：`1100x700`，用于对照 Codex 默认窗口比例。
- Codex 贴图参考尺寸记录：默认窗口约 `1100x700`；左侧栏约 `255px`；右侧主区约 `845px`；顶栏约 `52px`；当前 AI 输入框按本次对照标注收敛为 `400px` 宽、`93px` 高。
- 这些基准已固化在 `src/preset-manager/designMetrics.ts`，后续 UI 调整优先改设计标尺，不在组件里散落魔法数字。
- UI 方向：接近 Codex 的精密工具界面，强调简洁、干净、低噪音、半透明侧栏、弱边框、明确层级。
- 暗色模式不使用单一纯黑，使用多层黑灰建立空间。
- 预设条目走高级列表感，不走厚重卡片堆叠。
- AI 助手是内嵌命令入口，不是横跨全屏的外置聊天底栏。

## 当前节点：Codex 化视觉精修（P0/P1/P2）

目标：在已固化的标尺基础上，把暗色玻璃、AI 输入坞、预设条目和批注工具栏全部对齐 Codex 视觉语言，统一按钮和滚动条质感。

- [x] P0 玻璃化与令牌重构：暗色 `--pm-bg` 改为 `oklch(0.155 0.008 255)`，新增 `--pm-bg-sidebar`、`--pm-bg-transparent`、`--pm-split-line`、`--pm-ai-capsule`、`--pm-control-highlight`、`--pm-send-bg`、`--pm-btn-hover/active/active-border` 等令牌；浅色同步更新。
- [x] P0 主容器去重压：`.app-root::before` 去掉两组径向 glow 和内阴影，只保留线性渐变 + `blur(32px) saturate(140%)`；侧栏边缘线收回；工作区改用更深底色让玻璃侧栏漂浮其上。
- [x] P0 顶栏轻量化：去除底色和底部分隔线；`TitleBar` 按钮接入 `--pm-btn-size / --pm-btn-radius / --pm-btn-hover`，关闭按钮改 CSS 类替代 Tailwind 内联红色。
- [x] P1 AI 输入坞结构重做：拆出双行结构（顶部 input + 底部工具行），占位符改为 `Ask Codex anything`；模型胶囊靠左 + 自动占位，独立小窗按钮改为 hover 才显形的 `.floating-detach`，发送按钮换成 oklch 亮白圆。
- [x] P1 预设条目去卡片：展开态去掉 1px 边和圆角，改为 `border-top/bottom` 配 `var(--pm-row-active)` 底色 + 左侧 2px 强调条，列表更接近 Codex。
- [x] P1 面板小标题统一：工作台、收藏夹的图标去掉，文本改为 `草稿 / 收藏`，统一 11px 600 字重、`uppercase`、`letter-spacing: 0.04em`。
- [x] P1 批注工具栏紧凑分组：用 `.anno-group / .anno-separator` 重排工具/颜色/字号/操作；按钮缩到 22px，图标降到 10px；删除被覆盖的旧 hover 块。
- [x] P2 圆角与按钮尺寸令牌化：`--pm-btn-radius`、`--pm-btn-radius-pill`、`--pm-btn-size`、`--pm-btn-size-sm` 统一接入标题栏、AI 助手等组件。
- [x] P2 滚动条收窄：宽高从 10px 收到 6px，去除背景剪裁和边框，玻璃容器内更安静。
- [x] P2 Tailwind 警告清理：移除 `hover:!bg-red-500/30` 等内联危险样式，替换为 scoped CSS。
- [x] 同步更新 `visualPolish.test.ts` 断言以覆盖新令牌、滚动条 6px、面板小标题 uppercase、AI 占位符、`floating-detach`、条目去卡片边线、批注 `.anno-group` 与 22px 按钮等。
- [x] 从 UI 工作区同步开发者背景调试面板：支持本地背景图、毛玻璃、遮罩、渐变、噪点、内阴影、边缘高光、区域开关、预设保存和导入导出。
- [x] 新增 `devThemeCss/devThemeIO` 工具、Pinia 持久化 store、动态样式注入器和面板入口，覆盖 sidebar/workspace/panel 三个调试区域。
- [x] 新增 `devThemeCss.test.ts`、`devThemeIO.test.ts`、`devThemeIntegration.test.ts`，防止背景 CSS 生成、导入导出和 UI 挂载点回退。

## 已完成节点：Codex 参考尺寸标准化

目标：把参考图里的窗口、边栏、顶栏和 AI 输入框尺寸变成项目内统一标尺，后续“借鉴”Codex 时先对齐比例，再逐块改视觉。

- [x] 记录参考图尺寸：默认窗口 `1100x700`、侧栏 `255px`、主区 `845px`、顶栏 `52px`。
- [x] 记录 AI 输入框参考：曾按 Codex 贴图校准为约 `634px` 宽、`93px` 高，后按本次并排对照标注收敛为 `400px` 宽、`93px` 高。
- [x] 新增 `designMetrics.ts` 统一管理这些基准。
- [x] `App.vue`、`index.ts`、`TitleBar.vue`、`AiAssistant.vue` 改为引用统一基准或 CSS 变量。
- [x] 新增 `designMetrics.test.ts` 防止基准被误改。
- [x] 修正上一轮把截图画布尺寸误当小窗默认尺寸导致的 `1375x875` 突变问题。
- [x] AI 输入框保持 `634x93` 基准，并从底部 `26px` 下放到 `18px`。
- [x] AI 输入框宽度按本次截图标注从 `634px` 收窄到 `400px`。
- [x] 左侧栏暗色背景改为蓝灰玻璃层，减少纯黑压迫感。
- [x] 左侧栏进一步降低实底色，改为更轻的蓝灰玻璃层和柔和光影。
- [x] 修复侧栏透明感不生效的根因：根容器不再铺实体黑底，右侧工作区单独铺底，让左侧栏真正透到底层酒馆背景。
- [x] 暗色分割线、侧栏边界和条目细线略微提亮，增强 Codex 式细亮线观感。
- [x] 左侧栏内容整体上移，工作台和收藏夹标题、条目字号略放大。
- [x] 移除工作台和收藏夹条目里的内部横向分割线，保留 Codex 式低噪音列表感。
- [x] 左侧栏折叠按钮略向右移动，贴近 Codex 折叠按钮位置。
- [x] 修复批注工具栏在窗口尺寸变化后可能跑到可视区外，导致批注模式无法关闭的问题。

## 已完成节点：Codex 化 UI 主结构

目标：先把大比例和主要界面骨架定住，后续所有视觉微调都以这个结构为基准。

- [x] 小窗默认尺寸改为 `1100x700`。
- [x] 左栏默认宽度按最新实测调整到 `255px`。
- [x] 顶栏高度调整到接近 Codex 的 `52px`。
- [x] 左栏折叠后只保留折叠按钮，不残留侧栏宽度。
- [x] 左栏改成半透明、轻光影、弱边界，不使用粗分割线。
- [x] 第二预设展开后默认与主预设平分右侧工作区。
- [x] 分割线保持 `1px` 视觉线，隐藏命中区域保持易拖动。
- [x] 顶部按钮重新分组，低频功能收进更多菜单。
- [x] 修复标题栏预设下拉菜单和右侧更多菜单被主内容区遮盖的问题。
- [x] 按最新实测 Codex 截图校准侧栏、主区和 AI 输入框宽高比例。

## 下一节点：预设条目视觉重做

目标：让预设条目像 Codex 列表一样清晰、克制、可扫描。

- [x] 条目默认使用透明列表行 + 极淡分隔线。
- [x] hover 和当前项使用柔和高亮，不用厚边框。
- [x] 只有展开项使用轻微卡片化背景。
- [x] 标题、预览、状态点、收藏、展开箭头统一视觉轴线。
- [x] 条目右侧启用状态点改为胶囊开关，开启时带轻微绿色高亮。
- [x] 按批注取消展开项右侧的“放大/编辑”快捷图标，减少标题行按钮噪音。
- [x] 展开内容阅读区加高，方便直接查看完整提示词内容。
- [x] 展开底部操作区去掉和标题行重复的“禁用/收藏”，只保留编辑、复制/迁移、删除等低频操作。
- [x] 编辑弹窗从 `body` 传送改为面板内部浮层，避免点击编辑后预设列表视觉上消失或层级异常。
- [x] 标准模式继续以 `106%` 条目比例为基准。
- [x] UI 比例设置继续保留，可保存用户自定义比例并恢复默认。

## 下一节点：AI 助手重做

目标：把 AI 助手改成 Codex 式底部命令入口。

- [x] AI 输入框绑定在主预设区域底部，不横跨整个插件。
- [x] 输入框宽度按最新对照标注调整到约 `400px`，高度约 `93px`。
- [x] 使用半透明背景、弱边框、`blur(24px)` 和轻阴影。
- [x] AI 输入框背景进一步降实体感，按钮改为白色半透明高亮层，发送按钮改为明确高亮圆按钮。
- [x] 去掉笨重上栏，消息区域轻量浮出。
- [x] 设置、模型、发送按钮按 Codex 风格重新排布。
- [ ] 独立小窗模式保留，但拖动、吸附、收回和视觉反馈放到后续节点。

## 当前功能节点：AI 预设写作知识库与操作代理

目标：让插件 AI 不只是聊天建议，而是作为用户的预设写作 handler，能指导用户、帮助用户写条目，并在确认后代用户执行插件内部操作。

- [x] 新增中文知识库文档 `docs/ai-knowledge/preset-writing.md`，沉淀预设写作、诊断、文风、模型适配和安全边界。
- [x] 补充 `语言模型与提示词分析入门教程（雾）` 主附件的安全提炼：Transformer、decoder-only、文本向量化、自回归生成、上下文、token 和注意力诊断。
- [x] 新增运行时知识片段 `src/preset-manager/knowledge/presetWritingKnowledge.ts`，供插件 AI 在触发预设写作意图时注入。
- [x] `presetWritingSkill` 支持新手引导、从零创建、局部优化、故障诊断、文风塑形、模型适配六类处理策略。
- [x] `ai.ts` 在用户输入涉及预设写作时自动加入预设写作知识库提示。
- [ ] 设计 AI Action Agent：把用户指令解析为结构化动作，如切换预设、插入条目、修改条目、移动条目、开关条目。
- [ ] 动作执行前必须给预览和确认，不允许 AI 静默改动预设。
- [ ] 动作执行前写入历史快照，支持撤销。
- [ ] 第一版只开放低风险动作：切换当前预设、插入 AI 写好的条目、修改已有条目、移动条目、开关启用状态、复制到第二预设。
- [ ] 高风险动作暂缓：批量删除、自动清空、跨预设大规模重排。

## 当前功能节点：官方 Prompt Manager 兼容

目标：把插件的预设条目操作对齐 SillyTavern 官方 Prompt Manager，避免导入导出、未使用条目和系统条目处理出现格式偏差。

- [x] 新增 `officialPromptManager.ts`，统一处理官方 `identifier`、`prompt_order`、导入导出、默认顺序和系统/占位条目判定。
- [x] 官方导出格式改为 `{ version, type, data: { prompts, prompt_order } }`，其中 `prompts` 排除系统/占位条目，`prompt_order` 保留当前列表顺序和启用状态。
- [x] 官方导入支持 `data.prompts` 与 `data.prompt_order`，按 `identifier` 合并条目并应用启用状态和顺序。
- [x] 预设主列表只显示 `prompts`，`prompts_unused` 不再在主列表为空时混入显示。
- [x] 顶栏更多菜单新增“添加未使用条目”，可把 `prompts_unused` 中的条目移回当前列表，默认禁用并插到列表开头。
- [x] 条目操作拆分为“移出列表”和“删除”：移出列表保留条目到未使用池，删除才从 active/unused 中彻底删除。
- [x] 系统/占位条目禁止彻底删除、移出列表和跨预设迁移，避免破坏官方默认结构。
- [x] 插入、复制、迁移普通条目时重新生成 `id/identifier`，防止跨预设或重复插入造成官方 key 冲突。
- [x] 新增 `officialPromptManager.test.ts`、扩展 `promptOfficialTools.test.ts` 和 `promptOfficialFields.test.ts` 覆盖官方兼容行为。
- [x] 补齐预设级操作：新建预设、重命名预设、删除预设；复制预设底层保留，前端不显示。
- [x] 补齐系统提示词“恢复默认内容”能力，从 `default_preset` 恢复官方系统条目内容并保留当前启用状态。

## 下一节点：UI 批注系统完善

目标：让平板笔标注成为稳定沟通工具。

- [x] 批注内容可自动保存，关闭再打开后保留。
- [x] 批注工具支持清除。
- [x] 批注工具支持橡皮擦。
- [x] 默认颜色改为红色。
- [x] 工具栏缩小，并支持折叠。
- [x] 默认颜色只保留红、绿、蓝基础选项。
- [x] 标注点编号只按“标注点”自身递增，不被画笔、文字、框选影响。
- [x] 文字工具改成先框选文字范围，再输入。
- [x] 点击文字框外部或按 Enter 保存文字。
- [x] 保存后不显示文字框痕迹。
- [x] 再点文字可二次编辑。
- [x] 文字框范围可二次调整大小。
- [x] 新增标尺工具，可在截图上拖出测量线，显示长度、横向差值和纵向差值。
- [x] 文字批注支持字号调节，默认字号从 `22px` 调整为 `18px`，避免标注文字过压迫。
- [x] 工具栏坐标会自动夹回当前窗口可视范围，折叠后仍保留关闭按钮。

## 后续节点：历史备份系统增强

目标：把当前可用的历史系统继续做成可管理、可搜索、可恢复的备份系统。

- [x] 基础撤销、重做、历史记录和快照已存在。
- [ ] 将历史面板里的原生 `prompt/confirm` 换成项目内自定义弹窗。
- [ ] 快照搜索。
- [ ] 快照导出。
- [ ] 批量清理。
- [ ] 更细的自动备份策略。
- [ ] 恢复前增加差异预览。

## 后续节点：第二预设面板

目标：强化对比、迁移和冲突处理。

- [x] 第二预设展开和并排对照已存在。
- [x] 复制、迁移和重复关系标签已存在。
- [ ] 将跨面板重复确认和迁移确认从原生 `confirm` 换成项目内自定义弹窗。
- [ ] 文本差异对比。
- [ ] 批量复制。
- [ ] 批量迁移。
- [ ] 冲突处理界面进一步可视化。

## 后续节点：左侧工作台和收藏夹

目标：让左侧区域从“临时工具箱”升级为真正的整理工作台。

- [x] 基础插入和收藏能力已存在。
- [x] 工作台和收藏夹列表改为更轻的 Codex 式透明行、弱分隔和低噪音按钮。
- [ ] 将收藏夹删除确认从原生 `confirm` 换成项目内自定义弹窗。
- [ ] 草稿条目编辑。
- [ ] 草稿条目复制。
- [ ] 批量插入。
- [ ] 收藏分组整理。
- [ ] 收藏去重提示。
- [ ] 收藏条目编辑。

## 后续节点：排序和批量操作

目标：提升大量条目整理效率。

- [x] 拖拽排序已存在。
- [ ] 键盘排序。
- [ ] 批量移动。
- [ ] 更细的排序落点提示。
- [ ] 多选条目操作。

## 后续节点：规则符合性和代码结构整理

目标：让项目更稳定地符合酒馆助手开发规则，减少后续维护时的隐性冲突。

- [x] `AGENTS.md` 已从符号链接改为项目内真实文件。
- [x] 主入口使用 `$(() => ...)` 初始化，没有使用 `DOMContentLoaded`。
- [x] 主入口使用 `pagehide` 清理。
- [x] 独立 Vue 窗口使用 `createScriptIdIframe()` 创建 iframe。
- [x] iframe 内样式使用 `teleportStyle(iframeDoc.head)` 迁移。
- [x] 拖拽工具已处理触发元素 `ownerDocument` 和父页面 `document`。
- [x] 在 `AGENTS.md` 中补充“Codex 需要手动读取 `.cursor/rules/*.mdc`”的明确说明。
- [ ] 将 `manager.ts`、`history.ts`、`ai.ts` 的 `reactive` 单例 store 逐步迁移为 Pinia `defineStore`。
- [ ] 为脚本变量读取写入补充 zod 校验，优先覆盖 AI 配置、收藏夹、快照数据。

## 暂缓

- [ ] 导出批注图片到文件夹。当前优先保持界面内实时沟通，避免全屏、小窗移动和坐标换算带来误判。
- [ ] 完整 AI 独立小窗吸附系统。主 UI 稳定后再统一打磨。

## 已归档方案

- `docs/archive/plans/preset-manager-design-legacy.md`
- `docs/archive/plans/codex-ui-ai-drawer-plan.md`
- `docs/archive/plans/second-preset-transfer-plan.md`
