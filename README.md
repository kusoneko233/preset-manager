# Preset Manager

酒馆预设管理器，用来在独立窗口中查看、整理、编辑和备份 SillyTavern 预设里的 prompt 条目。

## 当前状态

当前推荐版本：`v0.7.1`

核心能力：

- 独立窗口和全屏模式，支持窗口拖动、四角缩放和位置保存。
- Codex 风格暗色/亮色 UI，支持预设条目比例、字体比例和内容预览行数设置。
- 主预设面板和第二预设面板，可展开对照。
- 预设条目查看、收藏、插入、编辑、删除、启用/禁用、拖拽排序。
- 操作历史、撤销/重做、指定历史恢复、手动/自动快照管理。
- AI 助手覆盖式输入框和展开面板基础能力。

## 版本记录

### v0.7.1

Codex 风格覆盖式 AI 助手和左侧分割线细化。

- 左侧主分割线改为贯穿窗口顶部，更接近 Codex 侧栏的连续分割方式。
- AI 助手改为覆盖在预设条目上方的输入框和聊天面板，不再占用预设列表底部空间。
- AI 输入框和展开面板使用半透明背景、细边框、柔和阴影和 `blur(24px)` 玻璃模糊效果。
- 优化 AI 助手输入区、发送按钮、空状态文案和展开面板视觉层级。
- 重新构建 `dist/preset-manager/index.js` 和 `dist/preset-manager/index.js.map`。

### v0.7.0

第二预设分屏和 AI 助手视觉优化。

- 第二预设面板展开时默认按预设工作区宽度五五分屏，不再固定为窄面板。
- 右侧分割线改为按实际容器宽度计算拖拽范围，移除 500px 上限造成的拉伸不足。
- 新增 `panelLayout` 布局计算工具和轻量测试脚本，避免分屏宽度逻辑回退。
- AI 助手从横跨整条底部改为居中限宽胶囊和居中展开面板，减少对预设区的压迫。
- 简化 AI 助手展开面板的上栏、对话气泡和输入区视觉层级，保留配置、关闭、脱出窗口和高度拖拽。
- 重新构建 `dist/preset-manager/index.js` 和 `dist/preset-manager/index.js.map`。

### v0.6.0

第二预设面板增强。

- 第二预设面板打开并选择预设后，展开条目可复制到另一侧预设。
- 支持将条目迁移到另一侧预设，迁移会同时从原预设移除。
- 条目行显示跨面板重复关系标签：同名、同内容、已存在。
- 跨面板复制或迁移遇到重复关系时会先确认，减少误重复插入。
- 历史系统支持单次操作记录多个预设变化，迁移操作可一次撤销/重做完整恢复。
- 新增 `promptRelations` 工具和轻量测试脚本，覆盖重复关系判断。
- 重新构建 `dist/preset-manager/index.js` 和 `dist/preset-manager/index.js.map`。

### v0.5.1

文档语言修正。

- 将 `codex-ui-ai-drawer-plan.md` 从英文改为中文，方便后续直接阅读和维护。
- 后续仓库内新增的 `.md` 文档默认使用中文。

### v0.5.0

Codex 式工作台和 AI 抽屉重构。

- 将 AI 助手默认状态改为预设工作区底部输入胶囊，展开后才占用抽屉高度。
- AI 抽屉输入栏固定在底部，避免高度较小时输入框被压没。
- 优化暗色主题层级，降低纯黑压迫感，让主工作区更轻、更清晰。
- 将预设条目从卡片式改为列表行式，使用细分隔线、悬停层和展开态区分条目。
- 精简标题栏、分割线、第二预设按钮和左侧栏折叠按钮的视觉重量。
- 调整左侧栏光影渐变，让它更接近 Codex 风格的自然侧栏氛围。
- 重新构建 `dist/preset-manager/index.js` 和 `dist/preset-manager/index.js.map`。

### v0.4.0

新增预设条目拖拽排序。

- 主预设面板和第二预设面板支持同面板内拖拽调整条目顺序。
- 排序动作接入撤销历史，可通过撤销/重做恢复排序前后的状态。
- 拖拽时显示细线落点提示，跨面板、收藏夹和草稿拖入仍保持复制插入逻辑。
- 支持将外部拖入条目插入到指定落点位置，而不是只能追加到末尾。
- 重新构建 `dist/preset-manager/index.js`。

### v1.3.0

历史备份系统增强。

- 历史面板显示完整操作记录，不再只看最近 10 条。
- 支持从历史面板回到任意操作记录后的状态，恢复动作会写入撤销历史。
- 快照支持创建时命名、重命名、恢复确认和删除确认。
- 自动快照按单个预设保留最近 8 个，避免长期使用后快照堆积过多。
- 重新构建 `dist/preset-manager/index.js`。

### v1.2.1

仓库文档和发布说明维护。

- 新增 README，记录项目能力、版本号规则和仓库维护流程。
- 补充各版本更新内容，方便后续通过 tag 回看变更。
- 明确 `src/preset-manager` 和 `dist/preset-manager` 都需要提交。

### v1.2.0

新增预设条目管理能力。

- 新增条目编辑弹窗，可修改名称、角色、启用状态和内容。
- 新增条目启用/禁用操作。
- 新增条目删除操作，删除前确认，可通过撤销恢复。
- 编辑、删除、启用/禁用全部接入撤销历史。
- 修复 `prompts_unused` 中条目无法被更新或删除的问题。
- 重新构建 `dist/preset-manager/index.js`。

### v1.1.1

UI 比例和布局细节修复。

- 新增可保存的紧凑、标准、放大 UI 设置档位。
- 标准模式下预设条目比例基准调整为 `106%`。
- 优化预设条目大小、内容预览行数和比例滑杆。
- 优化左侧栏、分割线和 AI 底栏视觉连接。
- 减少界面线条厚重感，让整体更接近 Codex 风格。
- 重新构建 `dist/preset-manager/index.js`。

### v1.1.0

Codex 风格 UI 重构。

- 重构整体暗色主题和亮色主题。
- 将 AI 助手底栏限制在预设工作区内。
- 优化左侧栏光影、标题栏、按钮组和面板布局。
- 调整分割线和窗口化体验。
- 重新构建 `dist/preset-manager/index.js`。

### v1.0.0

稳定基线版本。

- 修复 iframe 内拖拽监听源文档问题。
- 窗口拖动、四角拉伸、左右分割线、AI 底栏高度拖动恢复基础可用。
- 建立后续 UI 重构前的 GitHub 存档点。

## 版本号规则

本项目按语义化版本思路维护：

- `MAJOR`：破坏兼容性的结构变化，例如数据结构不兼容、安装方式大改。
- `MINOR`：新增功能，例如新增编辑器、备份系统、AI 独立窗口。
- `PATCH`：Bug 修复、UI 细节、文档和不破坏行为的小改动。

示例：

- `v1.2.1`：修复历史面板显示问题。
- `v1.3.0`：新增完整历史恢复功能。
- `v2.0.0`：更换数据存储结构且旧版本无法直接兼容。

## 仓库维护流程

每完成一项明确更新后执行：

```bash
pnpm build
git diff --check
git status --short
git add <changed-files>
git commit -m "<type>: <summary>"
git tag -a vX.Y.Z -m "vX.Y.Z <release title>" -m "<release notes>"
git push origin main vX.Y.Z
```

建议：

- `src/preset-manager` 和 `dist/preset-manager` 都提交。
- `src` 是可维护源码。
- `dist` 是酒馆实际可加载的构建产物，方便直接安装和回档。
- 每个 tag 都写清楚新增、修复和构建情况。
- GitHub Release 可以基于 tag 发布，Release notes 复制对应版本记录即可。

## 待完善路线

- 历史备份系统：后续可继续补快照搜索、导出、批量清理和更细的自动备份策略。
- 预设排序：后续可继续补键盘排序、批量移动和更细的排序落点提示。
- 第二预设面板：后续可继续补文本差异对比、批量复制和批量迁移。
- 左侧工作台：完善草稿条目编辑、复制和批量插入。
- 收藏夹：完善分组整理、去重提示和收藏条目编辑。
- AI 助手：支持独立小窗、拖动、靠边吸附和收回底栏。
